import { HabilidadesPokemon } from './habilidades-pokemon';

describe('HabilidadesPokemon', () => {
  it('should create an instance', () => {
    expect(new HabilidadesPokemon()).toBeTruthy();
  });
});
